请你告诉我2021年英雄联盟的全球总决赛的冠军战队，以及哪名队员被称为通天代？

### Prerequisites

- **Node.js and npx** (required for MCP server in notebook 3):
```bash
# On Ubuntu/Debian:
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# Verify installation:
node --version
npx --version
```